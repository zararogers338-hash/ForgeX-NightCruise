# ForgeX NightCruise v7.1 Industrial — Core Dependencies
# =======================================================
# Recommended Python: 3.10 or 3.11

# Desktop UI
customtkinter>=5.2.0
matplotlib>=3.7.0

# Document parsing
pypdf>=3.0.0
python-docx>=0.8.11

# HTTP client for Ollama / OpenAI-compatible endpoints
requests>=2.28.0

# CPU/RAM monitoring
psutil>=5.9.0

# NVIDIA GPU monitoring. Imported as `pynvml` in Python.
nvidia-ml-py>=12.535.108

# Optional backends are intentionally not installed by default because they are large
# and hardware-specific. Install only the backend you actually use:
#
# GGUF / llama-cpp-python CPU:
#   pip install llama-cpp-python
#
# GGUF / llama-cpp-python CUDA 12.4:
#   pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu124
#
# GGUF / llama-cpp-python Apple Metal:
#   CMAKE_ARGS="-DGGML_METAL=on" pip install llama-cpp-python
#
# HuggingFace Transformers:
#   pip install transformers torch accelerate
#
# PyTorch CPU:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# PyTorch CUDA 12.4:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
