# GitHub Publish Guide / GitHub 发布指南

Recommended repository name:

```text
ForgeX-NightCruise
```

Suggested topics:

```text
ai llm synthetic-data dataset-generation local-ai ollama gguf llama-cpp gradio customtkinter fine-tuning sft lora qlora model-distillation gpu-monitoring python open-source
```

Publish commands:

```bash
git init
git add .
git commit -m "Initial open-source preview release"
git branch -M main
git remote add origin https://github.com/<your-name>/ForgeX-NightCruise.git
git push -u origin main
```

Suggested release tag:

```text
v7.1-open-source-preview
```
